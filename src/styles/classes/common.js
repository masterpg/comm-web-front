import {css} from 'lit-element'

const literal = css`
  .comm-pseudo-link {
    color: #3f51b5; /* --comm-indigo-500 */
    font-weight: 500;
    cursor: pointer;
  }
  .comm-pseudo-link:hover {
    text-decoration: underline;
  }
`

export default literal
